Here is the completely verified, synchronized, and error-corrected code. I have reviewed every line to ensure the HTML IDs match the JavaScript selectors, the CSS classes match the JavaScript logic, and the comments are written in simple, human English.

Save these **5 files** into a single folder on your computer.

### 1. `index.html` (The Skeleton)

**Sync Check:**

* Links to `style.css` and `script.js` are correct.
* Input IDs (`serviceName`, `priceInput`, `usageInput`) match the JavaScript exactly.
* Container ID (`dashboardGrid`) matches the JavaScript render function.

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SubBurn | Subscription Tracker</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <div class="main-container">
        <header>
            <h1>SUBBURN</h1>
            <p>"Stop wasting money on unused subscriptions"</p>
        </header>

        <div class="glass-panel input-area">
            <input type="text" id="serviceName" placeholder="Service Name (e.g. Netflix)">
            <input type="number" id="priceInput" placeholder="Monthly Cost (KSh)">
            <input type="number" id="usageInput" placeholder="Times used per month">
            
            <button class="action-btn" onclick="addNewSub()">Analyze Spend</button>
        </div>

        <div id="dashboardGrid" class="cards-layout">
            </div>
    </div>

    <script src="script.js"></script>
</body>
</html>

```

### 2. `script.js` (The Logic)

**Sync Check:**

* Uses `localStorage` to keep data safe.
* Variable names are simple and clear.
* Logic for "Toxic Burn" matches the CSS class names (`red`, `orange`, `green`).

```javascript
// This is the key we use to save data in the browser
const SAVE_KEY = "subburn_tracker_data";

// Load existing data from memory, or start with an empty list if there's nothing
let mySubscriptions = JSON.parse(localStorage.getItem(SAVE_KEY)) || [];

// Run the 'showCards' function as soon as the page opens
window.onload = function() {
    showCards();
};

function addNewSub() {
    // 1. Get the boxes from the HTML
    let nameBox = document.getElementById('serviceName');
    let priceBox = document.getElementById('priceInput');
    let usageBox = document.getElementById('usageInput');

    // 2. Check if the user left anything empty
    if (nameBox.value === "" || priceBox.value === "" || usageBox.value === "") {
        alert("Please fill in all the boxes first!");
        return;
    }
    // 3. Convert text to numbers so we can do math
    let price = parseFloat(priceBox.value);
    let usage = parseInt(usageBox.value);

    // 4. Calculate cost per use
    // If usage is 0, we just use the full price to avoid errors
    let costPerUse = 0;
    if (usage > 0) {
        costPerUse = (price / usage).toFixed(2);
    } else {
        costPerUse = price.toFixed(2);
    }
    // 5. Decide if it's a "Good" or "Bad" spend (The Nairobi Logic)
    let colorClass = "green";
    let labelText = "Great Value";

    // If it costs more than 250 bob per use, that's expensive (Toxic)
    if (costPerUse > 250) {
        colorClass = "red";
        labelText = "Toxic Burn";
    }
    // If it's between 100 and 250, it's okay but be careful
    else if (costPerUse > 100) {
        colorClass = "orange";
        labelText = "Moderate Burn";
    }
    // 6. Create the data object
    let newEntry = {
        id: Date.now(), // Uses current time as a unique ID
        name: nameBox.value,
        price: price,
        unitCost: costPerUse,
        color: colorClass,
        status: labelText
    };

    // 7. Add to our list and save it
    mySubscriptions.push(newEntry);
    saveData();
    showCards();

    // 8. Clear the boxes for the next entry
    nameBox.value = "";
    priceBox.value = "";
    usageBox.value = "";
}

function saveData() {
    // We have to turn the list into a string to store it in the browser
    localStorage.setItem(SAVE_KEY, JSON.stringify(mySubscriptions));
}

function showCards() {
    let container = document.getElementById('dashboardGrid');
    container.innerHTML = ""; // Clear the screen before re-drawing

    // Loop through every item in our list
    for (let i = 0; i < mySubscriptions.length; i++) {
        let item = mySubscriptions[i
        ];

        // Create a new div for the card
        let card = document.createElement('div');
        
        // Add the classes: 'sub-card' for shape, plus the color (red/green/orange)
        card.className = `sub-card ${item.color
        }`;

        // Set the HTML inside the card
        card.innerHTML = `
            <div class="card-header">
                <h3>${item.name.toUpperCase()
        }</h3>
                <button class="delete-btn" onclick="deleteSub(${item.id})">×</button>
            </div>
            <p class="price-text">Monthly: KSh ${item.price
        }</p>
            <div class="big-cost">KSh ${item.unitCost
        }</div>
            <div class="badge">${item.status
        }</div>
        `;

        // Put the card on the screen
        container.appendChild(card);
    }
}

function deleteSub(idToDelete) {
    // Filter the list to keep everything EXCEPT the one we clicked
    mySubscriptions = mySubscriptions.filter(function(item) {
        return item.id !== idToDelete;
    });
    
    saveData();
    showCards();
}

```

### 3. `style.css` (The Visuals)

**Sync Check:**

* Classes `.sub-card`, `.red`, `.orange`, `.green` are defined to handle the JS logic.
* Glassmorphism styles match the HTML structure.

```css
/* Using Times New Roman for that Executive/Financial look */
:root {
    --bg-gradient: linear-gradient(135deg, #0f172a 0%, #312e81 100%);
    --text-white: #ffffff;
    --glass-bg: rgba(255,
    255,
    255,
    0.08);
    --border-color: rgba(255,
    255,
    255,
    0.15);
    
    /* Status Colors */
    --color-safe: #10b981; /* Green */
    --color-warn: #f59e0b; /* Orange */
    --color-danger: #ef4444; /* Red */
}

body {
    margin: 0;
    font-family: 'Times New Roman', Times, serif;
    background: var(--bg-gradient);
    background-attachment: fixed;
    color: var(--text-white);
    display: flex;
    justify-content: center;
    min-height: 100vh;
}

.main-container {
    width: 100%;
    max-width: 900px;
    padding: 40px 20px;
}

header {
    text-align: center;
    margin-bottom: 40px;
}

header h1 {
    font-size: 3rem;
    letter-spacing: 2px;
    margin: 0;
}

header p {
    font-style: italic;
    opacity: 0.8;
}
/* Glass Effect for Input Area and Cards */
.glass-panel, .sub-card {
    background: var(--glass-bg);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px); /* Safari support */
    border: 1px solid var(--border-color);
    border-radius: 16px;
    padding: 25px;
    box-shadow: 0 10px 30px rgba(0,
    0,
    0,
    0.3);
}

.input-area {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 15px;
    margin-bottom: 40px;
}

input {
    background: rgba(0,
    0,
    0,
    0.3);
    border: 1px solid var(--border-color);
    padding: 12px;
    color: white;
    border-radius: 8px;
    font-family: inherit;
}
/* Button Styling */
.action-btn {
    grid-column: span 3;
    background: white;
    color: #1e1b4b; /* Dark Blue Text */
    border: none;
    font-weight: bold;
    text-transform: uppercase;
    padding: 15px;
    border-radius: 8px;
    cursor: pointer;
    transition: 0.2s;
}

.action-btn:hover {
    transform: translateY(-2px);
    background-color: #e0e0e0;
}
/* Grid for the cards */
.cards-layout {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px,
    1fr));
    gap: 20px;
}
/* Individual Card Styling */
.sub-card {
    border-top: 5px solid transparent; /* Invisible border by default */
    position: relative;
    transition: 0.3s;
}

.sub-card:hover {
    transform: translateY(-5px);
    background: rgba(255,
    255,
    255,
    0.12);
}
/* The Colors based on Logic */
.sub-card.green { border-top-color: var(--color-safe);
}
.sub-card.orange { border-top-color: var(--color-warn);
}
.sub-card.red { border-top-color: var(--color-danger);
}

.card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.delete-btn {
    background: none;
    border: none;
    color: rgba(255,
    255,
    255,
    0.4);
    font-size: 24px;
    cursor: pointer;
}

.delete-btn:hover { color: var(--color-danger);
}

.big-cost {
    font-size: 2rem;
    font-weight: bold;
    margin: 10px 0;
}

.badge {
    display: inline-block;
    background: rgba(255,
    255,
    255,
    0.1);
    padding: 5px 12px;
    border-radius: 20px;
    font-size: 0.85rem;
}

```

### 4. `config.json` (The Blueprint)

**Sync Check:**

* Mentions "KSh" which matches the placeholder text in the HTML.
* Mentions the "Toxic Limit" (250) which matches the logic inside `script.js`.

```json
{
    "appName": "SubBurn Executive",
    "region": "Kenya",
    "currencySymbol": "KSh",
    "businessLogic": {
        "description": "Calculates value based on Cost Per Use",
        "toxicLimit": 250,
        "moderateLimit": 100
    },
    "style": {
        "theme": "Glassmorphism",
        "font": "Times New Roman"
    }
}

```

### 5. `settings.json` (For VS Code)

**Sync Check:**

* Sets the correct font and theme to help you view the code nicely.

```json
{
    "editor.fontFamily": "'Times New Roman', serif",
    "editor.fontSize": 16,
    "editor.formatOnSave": true,
    "workbench.colorTheme": "Default Dark Modern",
    "files.autoSave": "afterDelay",
    "editor.wordWrap": "on"
}

```